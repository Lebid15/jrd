# خطة تطوير مشروع JRD — التحوّل إلى نظام جرد آلي شامل

> الهدف: تحويل نظام الجرد اليومي الحالي من إدخال يدوي إلى نظام **آلي بالكامل** عبر مزيج من: روبوتات تصفّح (Web Scraping/Automation)، تكاملات API، وبوت واتساب (Baileys). ثم إضافة طبقة **جرد شهري** فوق الجرد اليومي.

> **منهجية النقاش**: نناقش كل بند مستقلاً ونتفق على التفاصيل والـ schema قبل كتابة أي كود. كل بند سيُضاف إليه قسم تفصيلي لاحقاً في هذا الملف.

> ⚠️ **ملاحظة دائمة**: هذا الملف يُحدَّث باستمرار مع كل خطوة ننجزها. كل إنجاز يُعلَّم ✅ وكل تفصيل جديد يُضاف في مكانه.

---

## 📊 حالة المشروع — آخر تحديث: 4 يونيو 2026

| البند | الوصف | الحالة |
|-------|-------|--------|
| 1 | روبوت موقع شحن الألعاب (bayi.alayatl.com) | ✅ **منجز ومنشور** |
| 2 | روبوت موقع SMM (followers-store.com) | 🟡 **متوقف — مشكلة حجب Cloudflare** |
| 3 | روبوت البنوك | ⏳ لم يبدأ |
| 4 | بوت واتساب — البنية الأساسية | ⏳ لم يبدأ |
| 5 | مطابقة الصرافين | ⏳ لم يبدأ |
| 6 | مجموعة غلة + العامل الميداني | ⏳ لم يبدأ |
| 7 | نوعا الحوالات من المحلات | ⏳ لم يبدأ |
| 8 | الجرد الشهري | ⏳ لم يبدأ |
| 9 | إعادة هيكلة قاعدة البيانات | ⏳ لم يبدأ |
| 10 | الأمان والمراقبة + المصادقة | ⏳ مؤجل لآخر |

---

## ✅ سجل الإنجازات

### البند 1 — روبوت موقع شحن الألعاب (bayi.alayatl.com) — مكتمل
- ✅ بنية `scraper/` بـ Playwright (persistent context لحفظ الجلسة).
- ✅ تسجيل دخول تلقائي (رقم جوال + كلمة سر + PIN عشوائي).
- ✅ استخراج: `bakiye_toplami`, `borc_toplami`, `bayi_alacagi`, `toplam_bayi_sayisi`.
- ✅ قلب الإشارة: نعرض "benim_alacagim" = −bayi_alacagi.
- ✅ إعادة تسجيل دخول تلقائية عند جلسة منتهية (stale session بعد منتصف الليل).
- ✅ تكامل backend: نوع provider جديد `bayi_alayatl` في `providers.js` + `scrapers.js`.
- ✅ تكامل frontend: أيقونة بوت 🤖 زرقاء + حقول "رقم الجوال / كلمة السر" في إعدادات API.
- ✅ Docker + Railway: منشور على `ahlacard.net` مع Volume لحفظ بيانات الجلسة.
- ✅ `--no-sandbox` لـ Chromium (Railway يعمل كـ root).
- ✅ رسائل خطأ واضحة تظهر في الواجهة بدل "فشل جلب رصيد" المجردة.

---

## � البند 2 — موقع SMM (followers-store.com) — متوقف مؤقتاً

### ما أُنجز ✅
- إضافة نوع provider جديد `smm_panel` في `backend/src/providers.js` (دالة `fetchSmmPanelBalance`).
- يستخدم API الرسمي للموقع (endpoint `/api/v2`, action `balance`، يرجّع JSON بالرصيد بالـ USD).
- تطبيع `base_url` (يقبل الجذر أو مع `/api/v2`).
- إضافة الحقول في الواجهة (`ApiSettings.jsx`): base_url + api_token.
- يحفظ الرصيد في `usd_amount` (لأنه USD).
- محاولة 1: إرسال User-Agent متصفّح → لم يكفِ.
- محاولة 2: إضافة رؤوس متصفّح كاملة (sec-ch-ua, Origin, Referer) → لم يكفِ.
- محاولة 3: نشر **Cloudflare Worker** كوسيط (`cloudflare-worker/worker.js` + README) → فشل.

### المشكلة الحقيقية 🛑
- `followers-store.com` محمي بـ Cloudflare ويحجب IP خوادم Railway مباشرة (HTTP 403 + HTML).
- جرّبنا Cloudflare Worker وسيطاً، لكن Cloudflare **يمنع طلبات Worker → موقع آخر محمي بـ Cloudflare** (سياسة منع الحلقات) → الـ Worker يردّ 504 Gateway Timeout بعد 30 ثانية.
- اختبار يدوي من PowerShell: `curl` على رابط الـ Worker يفشل بـ schannel timeout بعد 30s — تأكيد قاطع أن المشكلة في الـ Worker وليس الكود.

### حالة المتغيرات الحالية على Railway
- `SMM_PROXY_URL = https://aged-moon-5d78smm-proxy.alayatl-tr.workers.dev` (الـ Worker موجود لكن غير مجدٍ).
- `SMM_PROXY_SECRET = jrd-2026-x9k2m7n4p8q1` (نفس القيمة موجودة في Cloudflare كـ `PROXY_SECRET`).
- يمكن إبقاؤها أو حذفها — سواء.

### الخيارات المقترحة للحل (نناقشها في المحادثة القادمة)

| # | الخيار | التكلفة | التعقيد | الموثوقية |
|---|--------|---------|---------|-----------|
| A | **Vercel Serverless Function** كوسيط (نفس فكرة الـ Worker، لكن Vercel على AWS وليس CF) | مجاني | منخفض (5 دقائق) | عالية |
| B | **Render Free Web Service** كوسيط | مجاني (cold start بطيء) | منخفض | متوسطة |
| C | **Playwright على Railway** — تسجيل دخول للوحة followers-store.com وقراءة الرصيد من الواجهة (مثل bayi_alayatl تماماً) | صفر إضافي | متوسط | عالية (بطيء 10–15s) |
| D | **بروكسي HTTP مدفوع** (Webshare/IPRoyal $1–5/شهر) | مدفوع | منخفض جداً | عالية جداً |
| E | **استضافة بديلة لـ backend** (مثل Hetzner/Contabo بـIP غير محجوب) | مدفوع | عالٍ (نقل المشروع) | عالية |

### القرار المبدئي
- المستخدم رفض الخيار المدفوع الشهري.
- **مرشّحان قويان**: A (Vercel) أو C (Playwright على نفس Railway).
- Vercel أسرع وأنظف، Playwright يحتاج بيانات دخول الموقع (Email + Password) لكن لا يحتاج خدمة خارجية.

### ملفات لها علاقة
- `backend/src/providers.js` — دالة `fetchSmmPanelBalance` (مع logs تشخيصية حالياً).
- `frontend/src/pages/ApiSettings.jsx` — حقول base_url + api_token.
- `cloudflare-worker/worker.js` + `cloudflare-worker/README.md` — الـ Worker (يمكن إعادة استخدام بنيته في Vercel).
- `info.md` — طريقة النشر (git push → Railway auto-deploy).

### الخطوة التالية في المحادثة القادمة
1. اختيار الحل (A / B / C / D).
2. لو A أو B: إعداد الحساب + نشر الوسيط + ربط المتغيرات.
3. لو C: تسليم بيانات دخول `followers-store.com` للعمل على scraper جديد.

---


## الوضع الحالي (Baseline)

- **مصادر بيانات الجرد اليوم**: يدوي + 3 مزودي API (Znet, Barakat, Murat Temiz).
- **الجرد**: يومي فقط، يُحفظ سعر صرف + بنود + إجمالي + ربح يومي مقارنة بالأمس.
- **لا يوجد**: واتساب، scraping، جرد شهري، تتبع مصادر الإيرادات، مطابقة صرافين.

---

## البنود الأساسية (Roadmap)

### 1. روبوت موقع شحن الألعاب (الخاص بنا)
- **الوصف**: موقعنا الذي يبيع شحنات الألعاب يحتوي صفحة تعرض جميع المحلات الموزّعة لدينا مع أرصدة كل محل + المجموع النهائي.
- **المطلوب**: روبوت يسجّل دخول إلى الموقع، يفتح صفحة المحلات، يجلب: `[اسم المحل، الرصيد، (مديونية إن وُجدت)]` + المجموع الكلي.
- **الإخراج**: تحديث بنود `type='shop'` (نوع جديد) في الجرد اليومي تلقائياً، وحفظ snapshot في جدول مستقل للأرشيف.
- **التقنية المرشّحة**: Playwright (أقوى من Puppeteer للمواقع المحميّة)، خدمة Node منفصلة `scraper/` أو داخل خدمة البوت نفسها.
- **نقاش لاحق**: عنوان الموقع، طريقة الدخول (user/pass/2FA؟)، شكل الجدول في الصفحة، تكرار الجلب (كل ساعة؟ عند الطلب؟).

---

### 2. روبوت موقع شحن المتابعين/المشاهدات (طرف ثالث)
- **الوصف**: موقع منفصل لا نملكه، نشتري منه خدمات SMM، نحتاج فقط **رصيدنا لديهم**.
- **المطلوب**: روبوت يسجّل دخول دورياً، يقرأ قيمة الرصيد فقط، يحدّث بنداً واحداً في الجرد.
- **الإخراج**: تحديث `current_values` لبند مرتبط `type='smm_provider'` (أو إعادة استخدام `provider` مع نوع جديد).
- **نقاش لاحق**: هل لديهم API رسمي؟ (أغلب لوحات SMM لديها API بمفتاح — يُفضّل API على scraping). اسم الموقع، آلية الدخول.

---

### 3. روبوت البنوك (Bank Scraping)
- **الوصف**: جلب أرصدة الحسابات البنكية تلقائياً.
- **المطلوب**: روبوت/روبوتات لكل بنك (قد يحتاج كل بنك driver منفصل) → قراءة الرصيد الحالي → تحديث بند `type='bank'`.
- **التحديات الأمنية**:
  - 2FA/OTP غالباً مطلوب → قد يحتاج تدخل يدوي أو ربط بـ SMS.
  - خطر إقفال الحساب لو اكتُشف automation.
  - تشفير كلمات السر في القاعدة (مثل ما فعلنا مع `auth_sessions`).
- **نقاش لاحق**: ما البنوك بالاسم؟ هل تدعم Open Banking API؟ هل يكفي تحديث يومي واحد قبل الجرد؟

---

### 4. بوت واتساب — البنية الأساسية (Baileys)
- **الوصف**: خدمة Node منفصلة بنفس المعمارية الموثّقة في [bothelp.md](bothelp.md).
- **المطلوب**:
  - إنشاء مجلد `bot/` (Node + Express + Baileys + AES-256-GCM لجلسات).
  - مفتاح مشترك `INTERNAL_API_KEY` بين البوت و backend.
  - single-tenant مبدئياً (`tenant_id="1"` ثابت) مع الإبقاء على بنية المسارات multi-tenant.
  - نقاط backend داخلية: `/api/internal/ingest`, `/api/internal/command`, `/api/internal/reconcile-log`.
  - صفحة واجهة: ربط واتساب (QR) + حالة الاتصال + logout.
- **نقاش لاحق**: رقم البوت الثانوي، اسم البوت الظاهر في "الأجهزة المرتبطة".

---

### 5. مطابقة الصرافين (Reconciliation — Exchangers)
- **الوصف**: لدينا صرافون نحن (نحوّل لهم) وصرافون لكم (يحوّلون لنا). كل عملية يجب أن **تتطابق** بين طرفين (نحن + الصراف) عبر رسائل واتساب.
- **المطلوب**:
  - تعريف "صراف" ككيان في DB مع تفاصيل (الاسم، المجموعة، العملة، النمط).
  - بوت يستقبل رسائل من مجموعة كل صراف ويستخرج: `[المبلغ، الاتجاه (نا/منا)، الوقت، المرجع]`.
  - شاشة مطابقة: قائمة بالعمليات غير المطابَقة، مع زر mark-as-matched يدوي + matching تلقائي بالمبلغ والوقت.
  - تنبيه بالفروقات (شبهة خطأ/تأخير/سرقة).
- **نقاش لاحق**: عدد الصرافين، شكل رسائلهم النموذجي، فترة السماح للمطابقة (±5 دقائق؟).

---

### 6. مطابقة العامل الميداني — مجموعة "غلة"
- **الوصف**: عامل يجمع نقداً من المحلات يدوياً ويكتب في مجموعة واتساب (اسمها "غلة") مثل: `استلمت 1500 ليرة من محل X`. لاحقاً يُسلّم النقود لصراف يكتب: `استلمت من [اسم العامل] 1500`.
- **المطلوب**:
  - parser لمجموعة "غلة" → سجل "تحصيلات العامل" `[المحل، المبلغ، التاريخ، حالة التسليم]`.
  - parser لمجموعات الصرافين → سجل "تسليمات للصراف".
  - مطابقة: مجموع تحصيلات العامل في الفترة = مجموع تسليماته للصرافين.
  - شاشة "كشف العامل" + تنبيه عند فرق.
- **نقاش لاحق**: أسماء العمال، أسماء المحلات، صيغة الرسائل النموذجية، هل العامل واحد أم عدة عمّال؟

---

### 7. نوعا الحوالات من المحلات
- **الوصف**: المحلات تُسدّد لنا بطريقتين:
  - **(أ) تحويل بنكي/أونلاين** → يظهر في بند البنوك (يُعالج في البند 3).
  - **(ب) نقد عبر العامل** → يظهر في كشف العامل (يُعالج في البند 6).
- **المطلوب**: ربط منطقي بين الطريقتين على مستوى **المحل**:
  - جدول `shops` يحتوي كل المحلات.
  - كل تحويل/تحصيل يُربط بـ `shop_id`.
  - شاشة "كشف محل" تجمع: ما عليه من شحن (من البند 1) + ما دفعه (بنكي + نقدي) = الرصيد المستحق.
- **نقاش لاحق**: هل المحلات في موقع شحن الألعاب (البند 1) هي نفسها التي تدفع نقداً، أم منفصلة؟ كيف نوحّد الأسماء؟

---

### 8. الجرد الشهري
- **الوصف**: قسم جديد في السايدبار بجانب "الجرد" الحالي (اليومي).
- **المطلوب**:
  - حساب آلي شهري بناءً على كل جرد يومي محفوظ خلال الشهر.
  - مؤشرات: إجمالي الربح الشهري، أعلى/أقل يوم، متوسط الربح اليومي، عدد أيام الخسارة.
  - تفصيل مصادر الإيرادات (شحن ألعاب / SMM / فروقات صرف).
  - تفصيل المصاريف (إن قرّرنا إضافة جدول `expenses`).
  - تقرير PDF شهري (مثل تقرير الأرشيف الحالي لكن مجمّع).
  - مقارنة بالشهر السابق.
- **نقاش لاحق**: ما الحقول الإضافية المطلوب رصدها لتمكين التقرير الشهري؟ هل نحتاج جدول `expenses`؟ هل نريد رسوماً بيانية (Recharts)؟

---

### 9. إعادة هيكلة قاعدة البيانات
- **الوصف**: مع كل التغييرات أعلاه، schema الحالي ضيّق.
- **الجداول الجديدة المتوقّعة** (مبدئياً، نُنقّحها في كل بند):
  - `shops` (المحلات: id, name, source_url_id, ...)
  - `workers` (العمّال الميدانيون)
  - `exchangers` (الصرافون)
  - `bank_accounts` (الحسابات البنكية)
  - `transactions` (كل عملية مالية: نوع، اتجاه، طرف، مبلغ، عملة، حالة مطابقة)
  - `whatsapp_groups` (تعريف المجموعات وربطها بـ entity)
  - `whatsapp_messages` (أرشيف خام للتشخيص)
  - `parse_rules` (قواعد regex لاستخراج البيانات من الرسائل)
  - `scrape_jobs` + `scrape_runs` (سجلّ تنفيذ الروبوتات)
  - `monthly_reports` (تقارير شهرية مُحفوظة)
- **نقاش لاحق**: نأخذ جدولاً واحداً مع كل بند بدل دفعة واحدة.

---

### 10. الأمان والمراقبة
- **المطلوب** (يُطبَّق بالتوازي مع كل بند):
  - تشفير بيانات حساسة (كلمات سر البنوك، tokens، جلسات واتساب).
  - logging مركزي (pino للبوت + winston/console للbackend).
  - تنبيهات (واتساب نفسه أو Telegram) عند: فشل scraping، انقطاع جلسة، فرق مطابقة كبير.
  - مصادقة أساسية على الواجهة (المشروع حالياً مفتوح بدون تسجيل دخول).
- **نقاش لاحق**: مستوى الأمان المطلوب (المشروع محلي/شبكة داخلية أم على الإنترنت؟).

---

## ترتيب التنفيذ المقترح (للنقاش)

نقترح المراحل التالية بترتيب يبني الواحد على الذي قبله:

1. **المرحلة A — بنية البوت + قاعدة بيانات موسّعة**
   - البند 4 (بوت واتساب الأساسي)
   - البند 9 (إعادة هيكلة DB — الأساسيات فقط)

2. **المرحلة B — جلب البيانات الآلي**
   - البند 1 (روبوت موقع شحن الألعاب) — الأهم
   - البند 2 (روبوت SMM)
   - البند 3 (روبوت البنوك)

3. **المرحلة C — المطابقات عبر واتساب**
   - البند 6 (مجموعة غلة + العامل)
   - البند 5 (الصرافون)
   - البند 7 (ربط المحلات والحوالات)

4. **المرحلة D — التقارير والتحليلات**
   - البند 8 (الجرد الشهري)
   - البند 10 (الأمان والمراقبة)

---

## الخطوة التالية

اختر بنداً واحداً من القائمة أعلاه لنبدأ النقاش التفصيلي حوله. أنصح بالبدء بـ:
- **البند 4** (بنية بوت واتساب) لأنه أساس لبنود 5 و 6 و 7.
- أو **البند 2** (روبوت SMM) أو **البند 3** (البنوك) إذا أردت إنجازاً مستقلاً أسرع.

سيتم توسيع كل بند هنا في هذا الملف بقسم تفصيلي يحوي: schema، نقاط API، مكوّنات UI، حالات الحافة، اختبارات، خطوات التنفيذ.

> 🔁 **تذكير**: هذا الملف يُحدَّث دائماً — كل ما ننجزه يُعلَّم ✅ وكل قرار جديد يُوثَّق هنا مباشرة.
